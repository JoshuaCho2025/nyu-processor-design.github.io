## Week of 21 March 2024
Setup:
  - Had to redo the setup of the development. Thought that the codes in the documents were supposed to be inputted
into the terminal of my computer. Finally realized that it was supposed to be in the built-in terminal in VSCode.
Typical Errors that I ran into was creating the ssh and initializing the repository. Never did anything like this before
and very new to VSCode so was not sure where to put what. Error that was occurred when trying to create the ssh was
that I did not realize the locations of the keys. Initally, tried to use the clip command but had to search for the .ssh
file instead. Also when looking for the file location, it was important to note that looking in the vscode ssh file did
not give the correct key but instead I looked for it in the Ubuntu file. There I was able to easily set up the ssh.
Additionally did not realize to first write the codes within the WSL remote connection. Fixed this and the rest of
the setup went smoothly. Next had to figure out how to properly initialize the respository. 
Lab #2:
  - Working on Lab #2 (cmake). First error that I ran into was figuring out exactly where to create the file for CML.
Ended up being in the folder under Ubuntu and not the normal VSCode folder. My assumption now is to create the lab files
and any necessary folders, directories, documents in the root file in Ubuntu. After figuring this out, wrote the code directly
into the CMakeLists.txt file but was able to figure out that I could just open the CML text file through VSCode and write the code
there. Next, ran the code with the CML file and ran into this error:


CMake Error at CMakeLists.txt:2 (project):
  No CMAKE_CXX_COMPILER could be found.

  Tell CMake where to find the compiler by setting either the environment
  variable "CXX" or the CMake cache entry CMAKE_CXX_COMPILER to the full path
  to the compiler, or to the compiler name if it is in the PATH.

  
According to the internet, might need to install g++ and then set the compiler path. Will be asking Vito. 
